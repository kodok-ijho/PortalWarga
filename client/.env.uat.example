# Browser-safe UAT reference only. Runtime UAT reads the protected, ignored
# client/uat.env through an explicit allowlist; normal/production keeps .env.
# Never commit the runtime file or load its privileged entries into the browser.
SUPABASE_URL=https://staging-project-ref.supabase.co
SUPABASE_UAT_PUBLISHABLE_KEY=replace-with-active-staging-publishable-key
