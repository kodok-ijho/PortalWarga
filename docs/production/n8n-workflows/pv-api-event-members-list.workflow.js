import { workflow, node, trigger, ifElse, expr, newCredential } from '@n8n/workflow-sdk';

const headers = { entries: [{ name: 'Content-Type', value: 'application/json; charset=utf-8' }, { name: 'Cache-Control', value: 'no-store' }] };
const webhook = trigger({ type: 'n8n-nodes-base.webhook', version: 2.1, config: { name: 'POST /portal-v1/events/members/list', position: [180, 220], parameters: { httpMethod: 'POST', path: 'portal-v1/events/members/list', authentication: 'none', responseMode: 'responseNode', options: { allowedOrigins: 'https://portal-warga.vercel.app,http://localhost:5173,http://127.0.0.1:5173', ignoreBots: true } } } });
const extract = node({ type: 'n8n-nodes-base.code', version: 2, config: { name: 'Extract App Token', position: [460, 220], parameters: { mode: 'runOnceForAllItems', language: 'javaScript', jsCode: `const x=$input.first()?.json??{},b=x.body??{},h=x.headers??{},now=new Date().toISOString(),request_id=h['x-request-id']||h['X-Request-Id']||b.request_id||'event_members_list_'+Date.now(),m=String(h['x-portal-authorization']||h['X-Portal-Authorization']||h.authorization||h.Authorization||'').match(/^Bearer\\s+(.+)$/i),fail=(statusCode,code,message,details={})=>({statusCode,response:{ok:false,data:null,error:{code,message,details},meta:{request_id,timestamp:now}}}); if(!m?.[1])return[{json:{valid:false,...fail(401,'UNAUTHORIZED','Sesi tidak ditemukan.')}}]; return[{json:{valid:true,token:m[1].trim(),body:b,request_id}}];` } } });
const tokenOk = ifElse({ version: 2.3, config: { name: 'Token Present?', position: [720, 220], parameters: { conditions: { options: { caseSensitive: true, leftValue: '', typeValidation: 'strict', version: 2 }, conditions: [{ leftValue: expr('{{ $json.valid }}'), operator: { type: 'boolean', operation: 'true' }, rightValue: true }], combinator: 'and' } } } });
const jwt = node({ type: 'n8n-nodes-base.jwt', version: 1, config: { name: 'Verify App JWT', position: [1000, 100], onError: 'continueRegularOutput', parameters: { operation: 'verify', token: expr('{{ $json.token }}'), options: { complete: false, ignoreExpiration: false, ignoreNotBefore: false, clockTolerance: 30, algorithm: 'HS256' } }, credentials: { jwtAuth: newCredential('PV App JWT') } } });
const profile = node({ type: 'n8n-nodes-base.supabase', version: 1, config: { name: 'Fetch Actor Profile', position: [1280, 100], alwaysOutputData: true, parameters: { resource: 'row', operation: 'getAll', tableId: 'profiles', returnAll: false, limit: 1, filterType: 'manual', matchType: 'allFilters', filters: { conditions: [{ keyName: 'id', condition: 'eq', keyValue: expr('{{ $json.sub }}') }] } }, credentials: { supabaseApi: newCredential('PV Supabase Service Role') } } });
const validate = node({ type: 'n8n-nodes-base.code', version: 2, config: { name: 'Authorize and Validate Event', position: [1560, 100], parameters: { mode: 'runOnceForAllItems', language: 'javaScript', jsCode: `const p=$input.first()?.json??{},r=$items('Extract App Token',0,0)?.[0]?.json??{},j=$items('Verify App JWT',0,0)?.[0]?.json??{},q=j.payload&&typeof j.payload==='object'?j.payload:j,now=new Date().toISOString(),id=r.request_id||'event_members_list_'+Date.now(),fail=(statusCode,code,message,details={})=>({allowed:false,statusCode,response:{ok:false,data:null,error:{code,message,details},meta:{request_id:id,timestamp:now}}}),event_id=r.body?.event_id??r.body?.id??'',uuid=/^[0-9a-f]{8}-[0-9a-f]{4}-[1-8][0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}$/i; if(q.iss!=='portal-palm-village'||q.aud!=='portal-palm-village-web'||!q.sub)return[{json:fail(401,'INVALID_TOKEN','Sesi tidak valid.')}]; if(!p.id||p.is_active!==true||p.approval_status!=='approved')return[{json:fail(403,'FORBIDDEN','Akun belum dapat mengakses endpoint ini.')}]; if(!uuid.test(event_id))return[{json:fail(400,'VALIDATION_ERROR','event_id harus UUID yang valid.',{field:'event_id'})}]; return[{json:{allowed:true,event_id,include_revoked:Boolean(r.body?.include_revoked??r.body?.includeRevoked),actor:{id:p.id,role:p.role},request_id:id}}];` } } });
const allowed = ifElse({ version: 2.3, config: { name: 'Request Authorized?', position: [1840, 100], parameters: { conditions: { options: { caseSensitive: true, leftValue: '', typeValidation: 'strict', version: 2 }, conditions: [{ leftValue: expr('{{ $json.allowed }}'), operator: { type: 'boolean', operation: 'true' }, rightValue: true }], combinator: 'and' } } } });
const members = node({ type: 'n8n-nodes-base.supabase', version: 1, config: { name: 'Fetch Event Members', position: [2120, 20], alwaysOutputData: true, parameters: { resource: 'row', operation: 'getAll', tableId: 'event_members', returnAll: true, filterType: 'manual', matchType: 'allFilters', filters: { conditions: [{ keyName: 'event_id', condition: 'eq', keyValue: expr('{{ $json.event_id }}') }] } }, credentials: { supabaseApi: newCredential('PV Supabase Service Role') } } });
const build = node({ type: 'n8n-nodes-base.code', version: 2, config: { name: 'Build Member List', position: [2400, 20], parameters: { mode: 'runOnceForAllItems', language: 'javaScript', jsCode: `const ctx=$items('Authorize and Validate Event',0,0)?.[0]?.json??{},rows=$input.all().map(i=>i.json??{}).filter(r=>r.id),admin=ctx.actor?.role==='admin',self=rows.filter(r=>r.profile_id===ctx.actor?.id&&!r.revoked_at); const fail=(statusCode,code,message,details={})=>({statusCode,response:{ok:false,data:null,error:{code,message,details},meta:{request_id:ctx.request_id,timestamp:new Date().toISOString()}}}); if(!admin&&!self.length)return[{json:fail(403,'FORBIDDEN','Akun tidak memiliki akses ke assignment event ini.',{event_id:ctx.event_id})}]; let visible=admin?rows.filter(r=>ctx.include_revoked||!r.revoked_at):self; visible=visible.map(r=>({id:r.id,event_id:r.event_id,profile_id:r.profile_id,assignment_role:r.assignment_role,assigned_at:r.assigned_at??null,revoked_at:r.revoked_at??null})); return[{json:{statusCode:200,response:{ok:true,data:{members:visible},error:null,meta:{request_id:ctx.request_id,timestamp:new Date().toISOString()}}}}];` } } });
const respond = node({ type: 'n8n-nodes-base.respondToWebhook', version: 1.5, config: { name: 'Respond Members List', position: [2680, 20], parameters: { respondWith: 'json', responseBody: expr('{{ $json.response }}'), options: { responseCode: expr('{{ $json.statusCode }}'), responseHeaders: headers } } } });
const error = node({ type: 'n8n-nodes-base.respondToWebhook', version: 1.5, config: { name: 'Respond Members Error', position: [2120, 240], parameters: { respondWith: 'json', responseBody: expr('{{ $json.response }}'), options: { responseCode: expr('{{ $json.statusCode }}'), responseHeaders: headers } } } });
export default workflow('pv-api-event-members-list', 'PV API - Event Members List').add(webhook).to(extract).to(tokenOk.onTrue(jwt.to(profile).to(validate).to(allowed.onTrue(members.to(build).to(respond)).onFalse(error))).onFalse(error));
